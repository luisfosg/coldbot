import { User } from '#/entity/user.entity'
import { Server } from '#/entity/server.entity'
import { UserServer } from '#/entity/user_server.entity'

export const entities = [
  User,
  Server,
  UserServer
]
